version: 2.1

orbs:
  architect: giantswarm/architect@9.0.1

workflows:
  test:
    jobs:
      - architect/go-build:
          context: architect
          name: go-build
          binary: klausctl
          filters:
            tags:
              only: /^v.*/
